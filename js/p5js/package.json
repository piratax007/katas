{
    "scripts": {
        "start": "browser-sync start --server '/home/src/code' --files 'index.html' --watch",
        "test": "jest test.js"
    },
    "dependencies": {
        "jest": "^29.5.0",
        "http-server": "^14.1.1",
        "browser-sync": "^2.29.1"
    }
}
